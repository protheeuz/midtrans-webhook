<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buat Tautan Pembayaran</title>
    <style>
        body {
            background-color: #1a1a2e;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
            color: #fff;
        }

        .container {
            text-align: center;
        }

        .logo {
            width: 100px;
            margin-bottom: 20px;
        }

        h1, h2 {
            margin: 0;
            padding: 0;
        }

        .card {
            background-color: rgba(0, 0, 50, 0.8);
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            width: 300px;
            margin: auto;
        }

        .card form {
            display: flex;
            flex-direction: column;
        }

        .card label {
            text-align: left;
            margin-bottom: 5px;
        }

        .card input {
            margin-bottom: 10px;
            padding: 10px;
            border: none;
            border-radius: 5px;
            background-color: rgba(255, 255, 255, 0.1);
            color: #fff;
        }

        .card input::placeholder {
            color: #ddd;
        }

        .card button {
            background-color: #e74c3c;
            color: white;
            border: none;
            padding: 10px;
            border-radius: 5px;
            cursor: pointer;
        }

        .card button:hover {
            background-color: #c0392b;
        }
    </style>
</head>
<body>
    <div class="container">
        <img src="/assets/logo.jpg" alt="Logo" class="logo">
        <h1>Mathtech Digital Studio</h1>
        <h2>Payment Management Systems</h2>
        <div class="card">
            <form action="/create-payment-link" method="post">
                <label for="customerName">Nama Pelanggan:</label>
                <input type="text" id="customerName" name="customerName" placeholder="Nama Pelanggan" required>
                
                <label for="phoneNumber">Nomor Telepon:</label>
                <input type="number" id="phoneNumber" name="phoneNumber" placeholder="6281234567890" required>
                
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" placeholder="email@domain.com" required>
                
                <label for="grossAmount">Jumlah Pembayaran:</label>
                <input type="number" id="grossAmount" name="grossAmount" placeholder="100000" required>
                
                <button type="submit">Buat Tautan Pembayaran</button>
            </form>
        </div>
    </div>
</body>
</html>